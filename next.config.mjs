/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['media.istockphoto.com','cdn-icons-png.flaticon.com', 't4.ftcdn.net'],
    },
};

export default nextConfig;
